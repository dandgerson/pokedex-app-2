import React from 'react'
import cl from 'clsx'

import s from './Legendaries.m.scss'

const Legendaries = () => (
    <div className={cl(s.root)}>
      <h1>Legendaries</h1>
    </div>
  )

export default Legendaries
